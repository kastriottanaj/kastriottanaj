# Build output
dist/
.astro/

# Dependencies
node_modules/

# Environment — never commit real keys
.env
.env.production
.env.local

# Lead database (lives on the server, not in the repo)
data/

# OS noise
.DS_Store
*.log
.vercel
.env*

# 546 MB 4K master left over from the retired Northbound page — never from the repo
public/travel/
